
# Matrix Generator (React + TypeScript + Tailwind)

A simple React + TypeScript application that dynamically generates matrices based on user-defined rows and columns, performs matrix operations, and displays the results.

---

## 🚀 Features

- Dynamic matrix generation using:
  - **Sum Matrix** → `left + right`
- Matrix addition functionality
- User-defined rows and columns
- Clean and reusable matrix view component
- Written in **React with TypeScript**
- Simple and beginner-friendly structure

---

## 🧠 How It Works

1. Enter the number of **rows** and **columns**
2. Click **Add Matrix**
3. The app generates:
   - Matrix A → Sum Matrix `(i + j)`
   - Matrix B → Multiply Matrix `(i × j)`
4. A result matrix is created by adding Matrix A and Matrix B
5. All matrices are displayed side by side

---

## 📂 Project Structure

